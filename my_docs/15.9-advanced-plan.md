# 进阶轮总览：my_src 全量对齐 src（第 16–26 章）

> 2026-09-13 批准开工。参考实现 `src/`（5558 行、12 文件），目标 my_src 功能等价。
> 完整版计划文件：`~/.claude/plans/warm-baking-nova.md`（本文件是教学视角版）。

## 已确认的决策

- **分工（混合，2026-09-13 加严）**：核心设计点用户亲手写；**小而完整的模块也不代填**（如 frontmatter.ts 这类，改为"给规格+骨架填空+分步提示"引导自写）；代填仅用于真正繁重的机械部分（大段提示词常量、配置合并、渲染边角）且逐处讲解
- **范围**：全量对齐；不移植 OpenAI 双后端（单 Anthropic 后端）
- **测试**：每章补 mock 场景 + 全量回归保持绿

## 工程事实（各章通用）

- run-mock 断言锚定**请求体**（system/messages/工具名）而非 stdout 装饰格式
- my_src 是 ESM（`"type": "module"`），chalk 5 已装
- `~/.mini-claude/...` 路径必须**运行时惰性求值**（mock 靠动态 import 前设 HOME/USERPROFILE 沙箱）
- ch4/7/8/9/11/15 六个旧 mock 场景将随各章迁移，迁移时重审断言
- withRetry 章需 `MINI_CLAUDE_SDK_MAX_RETRIES=0` 隔离 SDK 内置重试

## 章节序列

| 章 | 主题 | 核心交付 | 引导你写的部分 | mock 验证 |
|---|------|---------|---------------|----------|
| **16** | UI 层与 frontmatter 地基 | 新增 ui.ts、frontmatter.ts；agent/cli 接线（含 spinner 与流式首 token 协调） | ✅ 2026-09-17 完成：spinner 生命周期、diff 逐行分类循环（用户手写） | 无新场景，回归绿即过 |
| **17** | 多会话 + mock HOME 沙箱 | session.ts 重写（`~/.mini-claude/sessions/<id>.json`、metadata、listSessions、getLatestSessionId）；run-mock HOME 重定向 | session 全模块引导自写（含 getLatestSessionId 排序决策、类型契约） | ch4 迁移：新目录+metadata 断言 |
| **18** | read-before-edit 与 edit 质量 | readFileState(mtime)、read_file 行号、edit 唯一性+diff；executeTool 加 readFileState 参 | readFileState 判定策略（记/比/更新时机，写后回写防自伤） | 未 read 先 edit 报错；edit 成功；非唯一 old_string 落盘不变 |
| **19** | 权限流水线 | 规则文件（settings.json allow/deny，deny 连 --yolo 也拦）、checkPermission→{action,message} 8 阶段、5 权限模式、confirmedPaths | **8 阶段顺序决策**、matchesRule 通配语义；规则解析引导自写 | deny 规则拦 rm（带/不带 --yolo）+ allow 对照 |
| **20** | Agent 骨架 I：MCP/重试/token | McpManager 多 server+配置三处合并+15s 超时；withRetry 指数退避；token 四计数+费用+--max-cost/--max-turns；cache_control；close() | **withRetry 重试判定与退避**；withTimeout 无泄漏 race；budget 检查点选位 | MCP 双 server；max-cost 截停；retry 场景 |
| **21** | Agent 骨架 II：4 层压缩 | T1 budget→T2 snip→T3 microcompact→T4 auto-compact（token 0.85×有效窗口）；persistLargeResult；CONCURRENCY_SAFE_TOOLS 提前执行 | **四层判定逻辑与顺序**（utilization 分档、同文件旧读去重、冷缓存门控） | 大 usage 驱动 T2/T3；T4 沿 ch7 track；30KB 持久化 |
| **22** | Memory 重构 | 4 类型、`~/.mini-claude/projects/<sha256(cwd)前16>/memory`、写时自动重建 MEMORY.md、语义召回 sideQuery 选 ≤5、freshness、异步 prefetch+60KB 预算 | **召回契约与门控设计**（selector JSON 契约、isQuerySubstantial、prefetch 跨轮状态机）；frontmatter 应用引导自写 | ch8 迁移+selector side track |
| **23** | Sub-agent 与 Skills 完整版 | 3 类型+白名单+自定义 `.claude/agents/*.md`（project 覆盖 user）+token 回传；SKILL.md frontmatter、fork/inline、$ARGUMENTS、/skills | allowed-tools 白名单解析与过滤；inline/fork 双入口分流；SKILL.md 解析引导自写 | 自定义 agent 白名单断言；fork track；ch9/ch11 迁移 |
| **24** | /goal 三态与 /loop 两模式 | 三态 JSON 契约+parseGoalVerdict 容错+25 轮硬顶；parseLoopInput 三分支、interval+dynamic+schedule_wakeup、clampWakeupDelay[60,3600] | **parseGoalVerdict fail-closed 容错**；parseLoopInput 优先级边界 | ch15 goal 迁移；impossible 停机；loop tick 序列 |
| **25** | ToolSearch 与 Plan Mode 完整版 | deferred 机制+tool_search；plan 文件、enter/exit_plan_mode、4 选项审批、/plan toggle；runCli 注入 {confirmFn, planApprovalFn} | deferred 激活状态设计；exit 四选项状态迁移图 | plan 写入→exit→审批→执行；keep-planning 回灌 |
| **26** | Auto Mode 完整版与 CLI 收口 | 规则文件 fail-closed、fast-path、两段分类器（256/1024tok）、防注入 transcript、DENIAL_LIMITS 3/20；cli 全 flags+/cost /compact /memory /skills+SIGINT 两连退出 | **分类器 transcript 防注入设计**；DENIAL_LIMITS 降级决策 | stage1 拦/stage2 清；DENIAL_LIMITS 转 confirm；ch15 auto 迁移 |

依赖主线：16→17→18→19→20→21（需 20 token）→22（需 16/17）→23（需 16/20）→24→25→26。

## 当前进度与续作点

- ✅ 16 章完成（2026-09-17）：ui.ts 全部（用户手写 spinner 生命周期与 diff 分类循环，review 两轮后过）、frontmatter.ts、agent/cli 接线、回归 13/13 绿；讲解与收尾记录在 `my_docs/16-ui-frontmatter.md`
- ⏭️ **下次从这继续**：开 17 章（多会话 + mock HOME 沙箱）——session.ts 全模块引导自写，含 getLatestSessionId 排序决策
- 备注：frontmatter.ts 本次为代填示范，22 章 memory 会真正用到它——届时作为复习点引导重写/自查

## 验证节奏

- 每章：`npm run build` + 全量 mock 回归绿；新场景断言磁盘副作用
- 每 3–4 章真机冒烟一次（智谱 glm-5.3-flash，复用 ch14 方法）
- 26 章后：全量 mock + ch14 十三场景适配版真机复验
